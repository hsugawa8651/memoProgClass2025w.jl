#!/usr/bin/env node

/**
 * Build script for Shiki assets
 * This is a placeholder script that can be extended to pre-build Shiki assets if needed.
 * Currently, Shiki assets are generated dynamically by the Julia ShikiHighlighter module.
 */

console.log('📦 Shiki build script');
console.log('✨ Shiki assets will be generated dynamically during documentation build');
console.log('✅ Ready for documentation build');